import EventDetails from "@/components/(events)/[id]";
import React from "react";
import { StyleSheet } from "react-native";

const eventDetails = () => {
  return <EventDetails />;
};

export default eventDetails;

const styles = StyleSheet.create({});
