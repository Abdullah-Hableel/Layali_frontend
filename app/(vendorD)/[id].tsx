import VendorDetail from "@/components/(vendor)/[id]";
import React from "react";
import { StyleSheet } from "react-native";

const VendorDetails = () => {
  return <VendorDetail />;
};

export default VendorDetails;

const styles = StyleSheet.create({});
